<tr>
  <th scope="row"><%= test.id %></th>
  <th><%= test.title %></th>
  <th><%= test.questions.count %></th>
  <th>
    <%= button_to t('tests._test.button'), start_test_path(test), class: 'btn btn-primary' %>
  </th>
</tr>
