<h1><%= t('tests.index.header') %></h1>
<div>
  <table border="1" cellpadding="5" class="table">
    <thead>
      <tr>
        <h4>
          <th scope="col"><%= t('global_words.id_resource') %></th>
          <th scope="col"><%= t('tests.index.title') %></th>
          <th scope="col"><%= t('tests.index.questions') %></th>
          <th scope="col"><%= t('global_words.actions') %></th>
        </h4>
      </tr>
    </thead>
    <tbody>
      <%= render @tests %>
    </tbody>
  </table>
  <h4>
    <%= link_to t('.new_test'), new_admin_test_path, class: 'btn btn-primary' %>
  </h4>
</div>

